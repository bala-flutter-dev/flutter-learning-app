import 'package:flutter/material.dart';

class AppColors {
  // light theme colors
  static const primaryColor = Color(0xff1565c0);
  static const secondaryColor = Color(0xffeeeeee);

  static const primaryColor1 = Color(0xff1a237e);

  // dark theme colors
  static const darkThemePrimaryColor = Color(0xff212121);
  // static const darkThemePrimaryColor = Color(0xff);
}
